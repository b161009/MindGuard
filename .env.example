# Firebase client configuration is set in src/services/firebase/config.js for the current MVP.
# Turn this on only after starting the local_ai service on the same machine.
VITE_USE_LOCAL_AI=false
VITE_LOCAL_AI_URL=http://127.0.0.1:8000
